// If name of function change you need also add change here. ❌
describe("map()", () => {
  // Implementation details leaked. ❌
  it("it uses forEach loop to map", () => {});
});

// If name of component change you need also add change here. ❌
describe("<Button>", () => {
  // API shape mentioned. ❌
  it("when pass motive='primary' works correctly", () => {});
  // API shape mentioned. ❌
  it("consumes onClick callback function", () => {});
});
