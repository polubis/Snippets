// You can change name of function and it's still valid. ✔️
describe("Transforms one array to another when: ", () => {
  // No implementation details leak. ✔️
  it("loops over all items", () => {});
});

// Purpose of component is described. ✔️
describe("Allows to work with button when: ", () => {
  // No API shape leak. ✔️
  it("uses different motives", () => {});
  // No API shape leak. ✔️
  it("handles click", () => {});
});
